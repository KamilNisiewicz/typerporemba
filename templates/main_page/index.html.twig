{% extends 'base.html.twig' %}

{% block meta_title %}Strona główna
{% endblock %}
{% block meta_desc %}Typer EURO 2021 - zapraszam do zabawy! typerporemba.pl
{% endblock %}

{% block content %}
	<h2 class="prize-info">
		Całkowita pula do podziału dla zwycięzców
		<strong>{{ prizePool }}</strong>
		PLN
	</h2>
	{% if app.user %}
		{% if app.user.active %}
			<a href="{{ path('user') }}" class="tile tile-index">
				<i class="far fa-user"></i>
				<strong>Twoje konto</strong>
			</a>
		{% else %}
			{{ include('_partials/user_inactive_info.twig') }}
		{% endif %}
	{% else %}
		<div class="main-content">
			<div class="main-block main-text">
				Dziękujemy za uczestnictwo i pełne zaangażowanie w pierwszej edycji porembskiego typera!<br/>
				Emocji nie brakowało, a sprawa zwycięstwa ważyła się aż do ostatniego rzutu karnego mistrzostw!<br/>
				Zwycięzcom gratulujemy! Do następnego razu!
			</div>
			<div class="main-block main-form">
				<form method="post" action="{{ path('user_login') }}">
					<h3>Zaloguj się</h3>
					<div class="form-line">
						<label for="inputEmail">Login</label>
						<input type="text" value="" name="login" id="inputEmail" class="input-text" required/>
					</div>
					<div class="form-line">
						<label for="inputPassword">Hasło</label>
						<input type="password" name="password" id="inputPassword" class="input-text" required/>
					</div>
					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
					<button class="button-submit main-form-submit" type="submit">
						Zaloguj
					</button>
				</form>
				<div class="main-links">
					<a href="{{ path('user_register') }}" class="button-link main-button-link">Rejestracja</a>
					<a href="{{ path('user_reset_password') }}" class="button-link main-button-link">Zapomniałeś hasła?</a>
				</div>
			</div>
		</div>
	{% endif %}
{% endblock %}
